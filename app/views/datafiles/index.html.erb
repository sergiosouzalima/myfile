
<script>
$(document).ready(function() {
    $('input:visible:enabled:first').focus();
});
</script>

<div id="block" class="block">
  <div id="secondary-navigation" class="secondary-navigation">
    <ul class="wat-cf">
      <li class="first active"><%= link_to "#{t("web-app-theme.list", :default => "List")}", datafiles_path  %></li>
      <li><%= link_to "#{t("web-app-theme.new", :default => "New")}", new_datafile_path %></li>
    </ul>
  </div>          
  <div id="content" class="content">          
    <h2 class="title">
      <%= t("web-app-theme.all", :default => "All")  %>: 
      <%= Datafile.model_name.human.titleize.pluralize %>
    </h2>
    <div id="inner" class="inner">
	  <%= form_tag datafiles_path, :method => 'get' do %>  
	    <p>  
	      <%= text_field_tag :search, params[:search], :size => 100  %>  
	      <%= submit_tag "Pesquisar", :name => nil %> <br />
	      <span class="description">informe um valor para a pesquisa</span>
	    </p>  
	  <% end %>
      <table id="datafile_table" class="table" border=0>
        <tr>
          <th>
            <%= t("activerecord.attributes.datafile.name", 
               :default => t("activerecord.labels.name", :default => "Name")) %>
          </th>
          <th>
            Contato
          </th>
          <th>
            Contato
          </th>
          <th><%= t("web-app-theme.updated_at", :default => "Updated at")  %></th>
          <th class="last">&nbsp;</th>
        </tr>
		<%= render :partial => @datafiles, :locals => { :list => true } %>
      </table>
		
      <div class="actions-bar wat-cf">
        <div class="actions">
        </div>
        
        <%= will_paginate @datafiles, :previous_label => 'Anterior', :next_label => 'Próxima'  %>
        
      </div>
    </div>
  </div>
</div>

<% content_for :sidebar, render(:partial => 'sidebar') -%>