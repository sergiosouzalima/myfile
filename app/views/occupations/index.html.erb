<div class="block">
  <div class="secondary-navigation">
    <ul class="wat-cf">
      <li class="first active"><%= link_to "#{t("web-app-theme.list", :default => "List")}", occupations_path  %></li>
      <li><%= link_to "#{t("web-app-theme.new", :default => "New")}", new_occupation_path %></li>
    </ul>
  </div>          
  <div class="content">          
    <h2 class="title"><%= t("web-app-theme.all", :default => "All")  %> <%= Occupation.model_name.human %></h2>
    <div class="inner">
      <table class="table">
        <tr>             
          <th class="first">ID</th>
                    <th>
            <%= t("activerecord.attributes.occupation.name", :default => t("activerecord.labels.name", :default => "Name")) %>
          </th>
                    <th><%= t("web-app-theme.created_at", :default => "Created at")  %></th>
          <th class="last">&nbsp;</th>
        </tr>
        <% @occupations.each do |occupation| -%>
        <tr class="<%= cycle("odd", "even") %>">
          <td>
            <%= occupation.id %>
          </td>
                    <td>
            <%= link_to occupation.name, occupation_path(occupation) %>
          </td>
                    <td>
            <%= l(occupation.created_at, :format => :short) %>
          </td>
          <td class="last">
            <%= link_to "#{t("web-app-theme.show", :default => "Show")}", occupation_path(occupation) %> |
            <%= link_to "#{t("web-app-theme.edit", :default => "Edit")}", edit_occupation_path(occupation) %> |
            <%= link_to "#{t("web-app-theme.delete", :default => "Delete")}", occupation_path(occupation), :method => :delete, :confirm => "#{t("web-app-theme.confirm", :default => "Are you sure?")}" %>
          </td>
        </tr>          
        <% end -%>
      </table>
      <div class="actions-bar wat-cf">
        <div class="actions">
        </div>
        
        <%= will_paginate @organizations, :previous_label => 'Anterior', :next_label => 'Próxima'  %>
        
      </div>
    </div>
  </div>
</div>

<% content_for :sidebar, render(:partial => 'sidebar') -%>
